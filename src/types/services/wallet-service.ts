// types/services/WalletService.ts (normal TS types, assuming for service interfaces or something, but since not specified, perhaps empty or type defs)
export type RequestWithdrawalInput = {
  amount: number;
  paymentMethod: string;
  details?: any;
};
